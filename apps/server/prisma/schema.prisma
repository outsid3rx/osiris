generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Settings {
  id           String @id @default(cuid())
  passwordHash String
}

model Workflow {
  id      String @id @default(cuid())
  name    String
  content Json
}
